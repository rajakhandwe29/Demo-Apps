name: HTML-Executor Workflow

on:
  push:
    branches:
      - main

jobs:
  Demo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check HTML file
        run: |
          ls -la
          cat index.html